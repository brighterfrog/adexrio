"use strict";

function test() {
    
}

// const connex = new Connex({
//     node: 'https://mainnet.veblocks.net/', // veblocks public node, use your own if needed
//     network: 'main' // defaults to mainnet, so it can be omitted here
// })

// test get block: 11021974
// MESSAGE PAYLOAD:
