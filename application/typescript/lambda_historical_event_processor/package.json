{
  "name": "lambda_historical_event_processor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:tsc": "tsc",
    "local:dev": "NODE_ENV=dev ts-node-dev src/local.ts",
    "local:test": "NODE_ENV=test ts-node-dev src/index.ts --unhandled-rejections=strict",
    "local:prod": "NODE_ENV=prod ts-node-dev src/index.ts --unhandled-rejections=strict",
    "test": "NODE_ENV=dev mocha --exit --timeout 0 -r ts-node/register tests/**/*.spec.ts",
    "test:file": "NODE_ENV=prod mocha --exit --timeout 0 -r ts-node/register tests/**/dynamodb-event*.spec.ts",
    "lint": "eslint ./src",
    "lint:fix": "eslint ./src --fix"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.44.0",
    "@aws-sdk/lib-dynamodb": "^3.44.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.89",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.0.0",
    "@types/node": "^17.0.7",
    "@typescript-eslint/eslint-plugin": "^5.9.1",
    "@typescript-eslint/parser": "^5.9.1",
    "chai": "^4.3.4",
    "eslint": "^8.6.0",
    "eslint-config-google": "^0.14.0",
    "mocha": "^9.1.3",
    "ts-node": "^10.4.0",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.4"
  }
}
