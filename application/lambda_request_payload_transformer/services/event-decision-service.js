"use strict";

exports.processEventRecord =  async (event, context, lastBlockForEventsProcessed) => {  

}

// translateLastBlockToCurrentblock to event type
      // var s3prefix = getS3Prefix(record); 
      // transform payload
      // push to s3 bucket